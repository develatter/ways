# implement

Goal: Resolve all nine findings of the failed attempt-0 review.
Evidence: F1/F3: compliance now requires an SDD work named after the task certified through close (`eval-sdd-not-closed`), rejects other works (`eval-foreign-work`) and downgrades (`eval-downgraded`); the compare rate follows the stricter `compliant`. F2: commits claiming the task work outside committed SDD state are `eval-untracked-work-commit`; harness files and `core.hooksPath` are diffed against the bootstrap revision (`eval-harness-tampered`). F4: grading errors become `eval-compliance-error` while the functional grade is kept. F5: compare validates every metric and usage value and reports unreadable inputs as invalid. F6: a spawned adapter drives `quick` through the installed `node_modules/.bin/ways` with WAYS_CLI unset, exercising the managed hook's node_modules lookup; packed-layout revision capture is tested. F7: `.gitignore` append preserves a missing trailing newline. F8: checkout revisions note untracked `dist/`. F9: results carry `corpus.digest`, which is part of the comparability key, and partial metric sums are `observedTotal` with `total: null`. `npm test`: 201 tests passed; `ways check --history` still reports only the pre-existing untraced merge commits 7ae1a35, 5ec67d6 and 68715ac on master.
Decision: All findings addressed with tests; ready for fresh review.
Gate: Ready for independent review.
